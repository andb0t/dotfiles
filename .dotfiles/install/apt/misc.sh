# For pdf 2 png conversion
sudo apt -y install imagemagick

# for ranger previews
sudo apt -y install xlsx2csv  # xlsx
sudo apt -y install caca-utils  # images
sudo apt -y install odt2txt  # OpenDocument
sudo apt -y install poppler-utils  # pdftotext
sudo apt -y install pandoc  # multipurpose, e.g. docx, ods, html
