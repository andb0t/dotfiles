export VERBOSE=true

if "$VERBOSE"; then
  echo "Loading runcom/bash_profile.symlink"
fi

# If not running interactively, don't do anything

[ -z "$PS1" ] && return

# source bashrc

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

